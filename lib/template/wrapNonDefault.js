// defineMessages to keep the function as mapper
const defineMessages = (messages, final) => final ? (Object.entries(messages)) : messages

// entry start {{ENTRY}} //entry end

defineMessages(messages, true)